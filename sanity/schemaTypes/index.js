import { blockContentType } from "./blockContent";
import { post } from "./post";
import { strategy } from "./startegy";

export const schema = {
  types: [
    post,
    strategy,
    blockContentType
  ],
}
